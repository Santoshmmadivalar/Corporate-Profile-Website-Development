{
  "name": "outpro-india-monorepo",
  "version": "1.0.0",
  "description": "Monorepo workspace for Outpro.India corporate website",
  "private": true,
  "scripts": {
    "install:all": "npm install --production=false && (cd backend && npm install --production=false) && (cd frontend && npm install --production=false)",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\"",
    "seed": "cd backend && npm run seed",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
